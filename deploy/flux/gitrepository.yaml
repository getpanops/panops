apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: panops
  namespace: flux-system
spec:
  interval: 5m
  url: https://github.com/YOUR_ORG/panops.git  # CHANGE to your repo URL
  ref:
    branch: main
  # For private repos, add:
  # secretRef:
  #   name: panops-git-credentials
