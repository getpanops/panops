apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Secret 'clickhouse-password' in namespace 'security' must exist before applying.
# Created by: python3 deploy/configure.py
resources:
  - nous-intel/
  - sigma-scanner/
  - yara/
  - baseline-collector/
