apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Requires clickhouse-operator (deploy operators/ first).
# Secret 'clickhouse-password' in namespace 'observability' must exist before applying.
# Created by: python3 deploy/configure.py
resources:
  - keeper-cluster.yaml
  - clickhouse-cluster.yaml
  - clickhouse-s3-cnp.yaml
  - retention-migration-job.yaml
