apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Secret 'clickhouse-s3-credentials' in namespace 'observability' must exist before applying.
# Created by: python3 deploy/configure.py
resources:
  - gigapipe-service.yaml
  - gigapipe-deployment.yaml
