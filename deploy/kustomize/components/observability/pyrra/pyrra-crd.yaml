apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: servicelevelobjectives.pyrra.dev
spec:
  group: pyrra.dev
  names:
    categories:
    - pyrra
    kind: ServiceLevelObjective
    listKind: ServiceLevelObjectiveList
    plural: servicelevelobjectives
    shortNames:
    - slo
    singular: servicelevelobjective
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.target
      name: Target
      type: string
    - jsonPath: .spec.window
      name: Window
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServiceLevelObjective is the Schema for the servicelevelobjectives API
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: ServiceLevelObjectiveSpec defines the desired state of ServiceLevelObjective
            properties:
              alerting:
                description: Alerting configures the alerting rules generated from the SLO
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    type: object
                  disabled:
                    type: boolean
                  labels:
                    additionalProperties:
                      type: string
                    type: object
                  name:
                    type: string
                  page:
                    description: Severity for the page (critical burn rate) alert
                    properties:
                      labels:
                        additionalProperties:
                          type: string
                        type: object
                    type: object
                  ticket:
                    description: Severity for the ticket (warning burn rate) alert
                    properties:
                      labels:
                        additionalProperties:
                          type: string
                        type: object
                    type: object
                type: object
              description:
                type: string
              indicator:
                description: Indicator defines how to calculate the SLI
                properties:
                  bool_gauge:
                    description: BoolGauge is a simple boolean gauge (1=good, 0=bad)
                    properties:
                      grouping:
                        items:
                          type: string
                        type: array
                      metric:
                        type: string
                    required:
                    - metric
                    type: object
                  latency:
                    description: Latency uses a histogram metric to measure latency SLI
                    properties:
                      grouping:
                        items:
                          type: string
                        type: array
                      success:
                        description: MetricSelector selects the histogram bucket
                        properties:
                          metric:
                            type: string
                        required:
                        - metric
                        type: object
                      total:
                        properties:
                          metric:
                            type: string
                        required:
                        - metric
                        type: object
                    required:
                    - success
                    - total
                    type: object
                  ratio:
                    description: Ratio uses two counters to calculate error ratio
                    properties:
                      errors:
                        description: MetricSelector for error events
                        properties:
                          metric:
                            type: string
                        required:
                        - metric
                        type: object
                      grouping:
                        items:
                          type: string
                        type: array
                      total:
                        properties:
                          metric:
                            type: string
                        required:
                        - metric
                        type: object
                    required:
                    - errors
                    - total
                    type: object
                type: object
              labels:
                additionalProperties:
                  type: string
                type: object
              target:
                description: Target is the SLO target percentage as a string (e.g. "99.9")
                type: string
              window:
                description: Window is the rolling time window for the SLO (e.g. "4w")
                type: string
            required:
            - indicator
            - target
            - window
            type: object
          status:
            description: ServiceLevelObjectiveStatus defines the observed state of ServiceLevelObjective
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
