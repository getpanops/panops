apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Requires coroot-operator (deploy operators/ first).
# Secret 'coroot-clickhouse' in namespace 'observability' must exist before applying.
# Created by: python3 deploy/configure.py
resources:
  - coroot-cr.yaml
  - coroot-httproute.yaml
